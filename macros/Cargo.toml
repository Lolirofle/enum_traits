[package]
name = "enum_traits_macros"
version = "0.1.3"
authors = ["Lolirofle <lolipopple@hotmail.com>"]
description = "Syntax extension for the crate `enum_traits`"
#documentation = "..."
homepage = "https://github.com/Lolirofle/enum_traits"
repository = "https://github.com/Lolirofle/enum_traits.git"
#readme = "README.md"
keywords = ["enum","trait","traits"]
license = "MIT"

[lib]
plugin = true

[build-dependencies]
quasi_codegen = {version = "0.*",default-features=false}

[dependencies]
aster        = {version="0.*",default-features=false}
quasi        = {version="0.*",default-features=false}
quasi_macros = {version="0.*",default-features=false}
