[package]
name = "tests"
version = "0.0.0"
authors = ["Lolirofle <lolipopple@hotmail.com>"]

[dependencies]
enum_traits = {version="*",path="../lib",features=["nightly"]}
enum_traits_macros = {version="*",path="../macros",features=["nightly"]}
